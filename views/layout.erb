<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<link rel="stylesheet" href="stylesheets/styles.css">
	<title>Music Brunch Erstwhile</title>
</head>
<body>
	
	<% if @albums && @albums.length > 0 %>
		<div id="albums">
			<% @albums.each do |album| %>
				<a href="/albums/<%= album.id %>">
					<img src="<%= album.image_path %>" alt="<%= album.title %>" class="album">
				</a>
			<% end %>
		</div>
	<% end %>

	<%= yield %>

</body>
</html>
